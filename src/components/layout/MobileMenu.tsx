
import React from 'react';

// This component is no longer needed as mobile menu functionality 
// is now handled by the collapsible sidebar and header components
const MobileMenu = () => {
  return null;
};

export default MobileMenu;
